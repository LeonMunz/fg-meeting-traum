# FG Workspace — Home Domain

This document is the canonical domain reference for **Home**: the
personal workspace surface of the current user.

Companion documents:

- `docs/domain/foundation.md` — Work Items, assignment, blocked and
  completion semantics.
- `docs/domain/activity.md` — Activity: a separate parallel
  awareness/history feed.
- `docs/domain/authorization.md` — the read authorization Home
  projections must obey.
- `docs/CURRENT_STATE.md` — implementation checkpoint.

The invariants below are **settled decisions**. Code that contradicts
this document is an implementation defect.

## 1. Purpose and dominant invariant

Home is **personal**, for the current user: it answers "what do I
need to do now?". It is operational work for the user, **not**
management reporting and not a KPI/dashboard surface.

Home's module hierarchy:

1. **Needs attention**
2. **Today & next**
3. **My work**
4. **Continue working**

**Activity is a separate parallel awareness feed** (see
`docs/domain/activity.md`). It is never folded into Home module read
models, and Home read models never fold Activity rows into their
candidate sets.

## 2. Needs attention — implemented Work Item slice

`Needs attention` will eventually combine several candidate
families. Only the first objectively defined Work Item candidate
family is implemented (backend read model, no Home API endpoint, no
UI yet):

- **assigned, open Work Items that are overdue and/or blocked**.

Canonical semantics (reused, never re-interpreted):

- **Personal**: a Work Item qualifies only when the current user is
  currently assigned to it (`WorkItemAssignee`). Unassigned items,
  items assigned to someone else, and items the user merely owns the
  Project for never qualify.
- **Current read authorization is mandatory**: the identical
  current-membership boundary of personal My Work — current
  `ProjectMembership` (role `owner`/`member`) in the Work Item's
  Project AND current `ResearchGroupMembership` in the Project's
  Research Group. Losing Project or Research Group membership
  removes the Work Item from the read model immediately; historical
  assignment rows alone never grant visibility. The `owner`/`member`
  role set is the canonical assignee-eligible role set, not a Home
  rule: assignment requires the `PROJECT_WORK` capability
  (`owner`/`member` only; a viewer holds only `PROJECT_READ`), and
  the canonical mutation paths never leave an assignment row
  pointing at a user who lost eligibility (demotion to viewer or
  membership removal is blocked, or the assignments are atomically
  resolved in the same operation).
- **Completed Work Items are excluded**: a Work Item whose
  `status_definition.category` is `done` (canonical completion,
  `foundation.md` §12) is never a candidate, even with a stale past
  due date or stale blocked metadata.
- **Overdue**: the date-only `due_date` is **strictly before** the
  current Django application-timezone date. A due date of exactly
  today is NOT overdue. There is no user-specific timezone model.
- **Blocked**: canonical `blockedReason` rule (`foundation.md`
  §11) — present/non-empty → blocked; NULL/empty → not blocked.
  Blocked state is never inferred from Activity history or status
  names.

Read model contract:

- **One Work Item appears once**, with explicit stable
  machine-readable reason codes — `overdue` and `blocked`. An item
  that is both carries both codes. Display sentences are never
  generated by the read model.
- The candidate carries only composition-relevant data: Work Item
  identity, title, Project identity/name, due value, current
  semantic status category, blocked reason, and the reason codes.
  The full canonical Work Item remains reachable by ID.
- **Deterministic ordering** (single query, no hidden score):
  overdue items before blocked-only items; earliest `due_date`
  first (`NULLS LAST`); stable Work Item ID tie-break.
- **Bounded responsibility**: the read model provides candidates
  only. The later Home composition layer decides the global row
  limit, cross-domain prioritization, and summary counts.

Implementation: `apps/api/work_items/home_attention.py`
(`get_work_item_attention_candidates`), tested by
`apps/api/work_items/tests_home_attention.py`.

## 3. Deferred (documented direction, NOT implemented)

- **Due-soon candidates**: no canonical due-soon threshold exists in
  this repository; none is invented.
- **Meeting preparation** candidates.
- **Follow-up attention** candidates.
- Domain-specific Project-owner / decision problems.
- The Home aggregate API endpoint and any Home UI.
- The **Today & next** module, general **My work** module wiring,
  and **Continue working**.
