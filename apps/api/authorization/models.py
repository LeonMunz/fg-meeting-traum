"""The authorization foundation has no domain models of its own.

It is a pure server-side authorization kernel over the persisted
membership models (ResearchGroupMembership, ProjectMembership) and the
Meeting access relations.
"""
