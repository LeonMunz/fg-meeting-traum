[project]
name = "fg-api"
version = "0.1.0"
description = "FG Workspace REST API"
requires-python = ">=3.12"
dependencies = [
    "Django>=5.2,<5.3",
    "djangorestframework>=3.15,<4",
]

[project.optional-dependencies]
dev = []

[tool.django]
